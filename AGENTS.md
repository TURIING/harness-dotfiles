# 项目知识检索

项目知识按所属仓库分散在各自的 `doc/` 目录（由 har-doc 技能维护，每个仓库的 `doc/index.md` 是各自的注册表）。主仓库 `doc/index.md` 是总入口：含"知识库地图"，登记主仓库目录树内所有 doc/ 位置。每次用户提出问题或任务时：

1. 先读主仓库 `doc/index.md`：用知识库地图判断主题归属哪个仓库，再读该仓库的 `doc/index.md` 注册表定位相关主题文件
2. 只阅读与问题相关的主题文件，**不要阅读全部主题文件**
3. 回答涉及项目结构、架构、模块逻辑、业务流程时，优先参考对应 `doc/` 中的相关知识；没有对应文档时按正常方式处理（可提议用 har-doc 沉淀）
4. 发现 `doc/` 内容与代码现状不符时，提醒用户该文档可能已过期，不擅自修改（可提议用 har-doc 更新）
